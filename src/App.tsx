/**
 * Placeholder shell. The real router and layout arrive in T034; this exists so that
 * Phase 1's checkpoint - typecheck, lint and test passing on an empty project - is a
 * real assertion about a real build rather than a claim about nothing.
 */
export function App() {
  return (
    <main>
      <h1>Explainable AI Lab</h1>
      <p>Scaffold only. The lab arrives with User Story 1.</p>
    </main>
  )
}
